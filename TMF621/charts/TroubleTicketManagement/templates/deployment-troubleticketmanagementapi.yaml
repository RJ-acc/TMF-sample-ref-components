apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-troubleticketmanagementapi
  labels:
    oda.tmforum.org/componentName: {{ .Release.Name }}-{{ .Values.component.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      impl: {{ .Release.Name }}-troubleticketmanagementapi
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-{{ .Values.component.name }}
        impl: {{ .Release.Name }}-troubleticketmanagementapi
        version: {{ .Values.api.versionLabel }}
    spec:
      containers:
        - name: {{ .Release.Name }}-troubleticketmanagementapi
          image: {{ .Values.api.image }}
          env:
            - name: RELEASE_NAME
              value: {{ .Release.Name }}
            - name: COMPONENT_NAME
              value: {{ .Release.Name }}-{{ .Values.component.name }}
            - name: API_BASE_PATH
              value: /{{ .Release.Name }}-{{ .Values.component.name }}/tmf-api/troubleTicket/{{ .Values.component.apiVersion }}
            - name: ENGINE_SERVICE_URL
              value: http://{{ .Release.Name }}-troubleticketmanagementengine:8081
            - name: MONGODB_HOST
              value: {{ .Release.Name }}-mongodb
            - name: MONGODB_PORT
              value: {{ .Values.mongodb.port | quote }}
            - name: MONGODB_DATABASE
              value: {{ .Values.mongodb.database }}
            - name: NODE_ENV
              value: production
          imagePullPolicy: IfNotPresent
          ports:
            - name: {{ .Release.Name }}-pmapi
              containerPort: 8080
          startupProbe:
            httpGet:
              path: /{{ .Release.Name }}-{{ .Values.component.name }}/tmf-api/troubleTicket/{{ .Values.component.apiVersion }}/troubleTicket
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
