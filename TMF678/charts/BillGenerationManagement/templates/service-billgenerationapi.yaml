apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-billgenapi
  labels:
    app: {{ .Release.Name }}-{{ .Values.component.name }}
    oda.tmforum.org/componentName: {{ .Release.Name }}-{{ .Values.component.name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    impl: {{ .Release.Name }}-billgenapi
  ports:
    - port: 8080
      targetPort: {{ .Release.Name }}-bgapi
      name: http-{{ .Release.Name }}-billgenapi
