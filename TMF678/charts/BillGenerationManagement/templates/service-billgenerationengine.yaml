apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-billgenerationengine
  labels:
    app: {{ .Release.Name }}-{{ .Values.component.name }}
    oda.tmforum.org/componentName: {{ .Release.Name }}-{{ .Values.component.name }}
spec:
  type: ClusterIP
  selector:
    impl: {{ .Release.Name }}-billgenerationengine
  ports:
    - port: 8081
      targetPort: {{ .Release.Name }}-bgeng
      name: http-{{ .Release.Name }}-billgenerationengine
