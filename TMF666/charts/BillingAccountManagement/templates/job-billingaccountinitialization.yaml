apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-billingaccountinitialization
  labels:
    oda.tmforum.org/componentName: {{ .Release.Name }}-{{ .Values.component.name }}
spec:
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-billingaccountinitialization
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      restartPolicy: OnFailure
      containers:
        - name: {{ .Release.Name }}-billingaccountinitialization
          image: {{ .Values.billingaccountinitialization.image }}
          env:
            - name: RELEASE_NAME
              value: {{ .Release.Name }}
            - name: COMPONENT_NAME
              value: {{ .Release.Name }}-{{ .Values.component.name }}
            - name: ACCOUNT_API_BASE_URL
              value: http://{{ .Release.Name }}-billingaccountapi:8080/{{ .Release.Name }}-{{ .Values.component.name }}/tmf-api/accountManagement/{{ .Values.component.apiVersion }}
            - name: METRICS_CALLBACK_URL
              value: http://{{ .Release.Name }}-{{ .Values.component.name }}-sm:4000/listener/accountEvent
          imagePullPolicy: IfNotPresent
  backoffLimit: 10
