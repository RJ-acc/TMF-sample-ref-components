apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-productrecommendationinitialization
  labels:
    oda.tmforum.org/componentName: {{ .Release.Name }}-{{ .Values.component.name }}
spec:
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-productrecommendationinitialization
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      restartPolicy: OnFailure
      containers:
        - name: {{ .Release.Name }}-productrecommendationinitialization
          image: {{ .Values.productrecommendationinitialization.image }}
          env:
            - name: RELEASE_NAME
              value: {{ .Release.Name }}
            - name: COMPONENT_NAME
              value: {{ .Release.Name }}-{{ .Values.component.name }}
            - name: RECOMMENDATION_API_BASE_URL
              value: http://{{ .Release.Name }}-prodrecapi:8080/{{ .Release.Name }}-{{ .Values.component.name }}/tmf-api/recommendationManagement/{{ .Values.component.apiVersion }}
            - name: METRICS_CALLBACK_URL
              value: http://{{ .Release.Name }}-{{ .Values.component.name }}-sm:4000/listener/recommendationEvent
          imagePullPolicy: IfNotPresent
  backoffLimit: 10
