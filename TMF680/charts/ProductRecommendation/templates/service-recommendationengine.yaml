apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-recommendationengine
  labels:
    app: {{ .Release.Name }}-{{ .Values.component.name }}
    oda.tmforum.org/componentName: {{ .Release.Name }}-{{ .Values.component.name }}
spec:
  type: ClusterIP
  selector:
    impl: {{ .Release.Name }}-recommendationengine
  ports:
    - port: 8081
      targetPort: {{ .Release.Name }}-receng
      name: http-{{ .Release.Name }}-recommendationengine
