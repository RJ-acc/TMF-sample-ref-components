apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-partymanagementinitialization
  labels:
    oda.tmforum.org/componentName: {{ .Release.Name }}-{{ .Values.component.name }}
spec:
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-partymanagementinitialization
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      restartPolicy: OnFailure
      containers:
        - name: {{ .Release.Name }}-partymanagementinitialization
          image: {{ .Values.partymanagementinitialization.image }}
          env:
            - name: RELEASE_NAME
              value: {{ .Release.Name }}
            - name: COMPONENT_NAME
              value: {{ .Release.Name }}-{{ .Values.component.name }}
            - name: PARTY_API_BASE_URL
              value: http://{{ .Release.Name }}-partymanagementapi:8080/{{ .Release.Name }}-{{ .Values.component.name }}/tmf-api/partyManagement/{{ .Values.component.apiVersion }}
            - name: METRICS_CALLBACK_URL
              value: http://{{ .Release.Name }}-{{ .Values.component.name }}-sm:4000/listener/partyEvent
          imagePullPolicy: IfNotPresent
  backoffLimit: 10
