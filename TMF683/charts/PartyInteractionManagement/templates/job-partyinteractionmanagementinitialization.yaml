apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-partyinteractionmanagementinitialization
  labels:
    oda.tmforum.org/componentName: {{ .Release.Name }}-{{ .Values.component.name }}
spec:
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-partyinteractionmanagementinitialization
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      restartPolicy: OnFailure
      containers:
        - name: {{ .Release.Name }}-partyinteractionmanagementinitialization
          image: {{ .Values.partyinteractionmanagementinitialization.image }}
          env:
            - name: RELEASE_NAME
              value: {{ .Release.Name }}
            - name: COMPONENT_NAME
              value: {{ .Release.Name }}-{{ .Values.component.name }}
            - name: PARTY_INTERACTION_API_BASE_URL
              value: http://{{ .Release.Name }}-partyinteractionmanagementapi:8080/{{ .Release.Name }}-{{ .Values.component.name }}/tmf-api/partyInteractionManagement/{{ .Values.component.apiVersion }}
            - name: METRICS_CALLBACK_URL
              value: http://{{ .Release.Name }}-{{ .Values.component.name }}-sm:4000/listener/partyInteractionEvent
          imagePullPolicy: IfNotPresent
  backoffLimit: 10
