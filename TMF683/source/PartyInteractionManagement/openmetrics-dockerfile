FROM python:3.12-slim

WORKDIR /app

COPY source/PartyInteractionManagement/requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r /app/requirements.txt

COPY source/PartyInteractionManagement /app

ENV PYTHONPATH=/app

CMD ["uvicorn", "openMetricsMicroservice.app:app", "--host", "0.0.0.0", "--port", "4000"]
