apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-configurationengine
  labels:
    app: {{ .Release.Name }}-{{ .Values.component.name }}
    oda.tmforum.org/componentName: {{ .Release.Name }}-{{ .Values.component.name }}
spec:
  type: ClusterIP
  selector:
    impl: {{ .Release.Name }}-configurationengine
  ports:
    - port: 8081
      targetPort: {{ .Release.Name }}-cfgeng
      name: http-{{ .Release.Name }}-configurationengine
