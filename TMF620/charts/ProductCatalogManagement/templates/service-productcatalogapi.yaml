apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-productcatalogapi
  labels:
    app: {{ .Release.Name }}-{{ .Values.component.name }}
    oda.tmforum.org/componentName: {{ .Release.Name }}-{{ .Values.component.name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    impl: {{ .Release.Name }}-productcatalogapi
  ports:
    - port: 8080
      targetPort: {{ .Release.Name }}-pcapi
      name: http-{{ .Release.Name }}-productcatalogapi
