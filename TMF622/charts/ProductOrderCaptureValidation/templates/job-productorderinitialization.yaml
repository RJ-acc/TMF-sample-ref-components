apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-productorderinitialization
  labels:
    oda.tmforum.org/componentName: {{ .Release.Name }}-{{ .Values.component.name }}
spec:
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-productorderinitialization
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      restartPolicy: OnFailure
      containers:
        - name: {{ .Release.Name }}-productorderinitialization
          image: {{ .Values.productorderinitialization.image }}
          env:
            - name: RELEASE_NAME
              value: {{ .Release.Name }}
            - name: COMPONENT_NAME
              value: {{ .Release.Name }}-{{ .Values.component.name }}
            - name: ORDER_API_BASE_URL
              value: http://{{ .Release.Name }}-prodorderapi:8080/{{ .Release.Name }}-{{ .Values.component.name }}/tmf-api/productOrderingManagement/{{ .Values.component.apiVersion }}
            - name: METRICS_CALLBACK_URL
              value: http://{{ .Release.Name }}-{{ .Values.component.name }}-sm:4000/listener/orderEvent
          imagePullPolicy: IfNotPresent
  backoffLimit: 10
