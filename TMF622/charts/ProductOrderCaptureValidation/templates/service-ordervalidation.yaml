apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ordervalidation
  labels:
    app: {{ .Release.Name }}-{{ .Values.component.name }}
    oda.tmforum.org/componentName: {{ .Release.Name }}-{{ .Values.component.name }}
spec:
  type: ClusterIP
  selector:
    impl: {{ .Release.Name }}-ordervalidation
  ports:
    - port: 8081
      targetPort: {{ .Release.Name }}-oval
      name: http-{{ .Release.Name }}-ordervalidation

